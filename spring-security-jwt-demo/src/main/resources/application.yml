spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.0.18:5432/drawhale
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
      format_sql: true
      database: postgresql
  #    database-platform: org.hibernate.dialect.PostgresSQLDialect

logging:
  level:
    org.springframework.jdbc: INFO
    org.springframework.security: INFO
